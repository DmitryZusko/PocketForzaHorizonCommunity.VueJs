interface IImageSize {
  width: number;
  height: number;
}

export const defaultLogoSize: IImageSize = {
  width: 150,
  height: 100,
};

export const defaultPageSize = 12;

export const defaultTextPreviewLimit = 100;
